{
  "name": "moonpool-example-mock-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser dev host embedding the hello mini app over an iframe Portal.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@moonpool/host": "workspace:*",
    "@moonpool/protocol": "workspace:*",
    "@moonpool/transport-iframe": "workspace:*"
  }
}
